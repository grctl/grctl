nats:
  server_name: grctl
  mode: embedded
  port: 4225
  store_dir: ~/.grctl/data
  sync_interval: always
  storage: file
  config_file: ""        # optional: path to nats.conf for advanced settings

defaults:
  worker_response_timeout: "5s"
  step_timeout: "5m"
